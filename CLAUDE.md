# Vocal Training — Project Notes

Personal vocal-warmup app. Goal: pitch detection + accuracy scoring with piano accompaniment for daily warmups, deployable to TestFlight (iOS) with web as a free byproduct.

## Design System

**Always read `DESIGN.md` before making any visual or UI decision.** All font choices, colors, spacing tokens, motion specs, and aesthetic direction are defined there — do not deviate without explicit user approval. If you ship code that uses hex literals, font sizes, or spacing values not defined in DESIGN.md, that is a regression — fix it before declaring done. In QA mode, flag any UI code that doesn't match DESIGN.md.

## Stack

- **Expo SDK 54** (`expo: ~54.0.33`), Expo Router 6, TypeScript strict
- **`react-native-audio-api@0.11.7`** (Software Mansion, pinned — 0.12+ has iOS audio session breaking change)
- **`pitchy@^4.1.0`** for pitch detection (MPM)
- **`tone@^15`** for web piano synthesis (web only — Tone.js does not work on RN per issue #1319)
- **`@react-native-async-storage/async-storage`** for session persistence (web shim → localStorage)
- **Salamander grand piano samples** — web bundles the full 21-sample set (every minor third A1–A6, ~1.4 MB) in `public/salamander/`, served same-origin (no `tonejs.github.io` CDN dependency; works offline after first load). `player.web.ts` `SALAMANDER_BASE = "/salamander/"`. Native iOS bundles an 8-note subset (~668 KB) in `assets/salamander/` decoded once on init.

The PRD lives at `prd-guidance-for-vocal-app.md` — authoritative reference for pedagogy, exercise table, accompaniment patterns, pitch-detection caveats. Read it before making music-theoretic changes.

**ROADMAP.md is the source of truth for what's shipped and what's next.** Keep it and this file in sync as part of any feature-slice PR — not as a follow-up.

## Architecture

```
data/exercises/*.json         # 10 starter exercises (JSON descriptors)
assets/salamander/*.mp3       # 8 bundled samples (A1, C2, A2, C3, A3, C4, A4, C5)
lib/exercises/types.ts        # ExerciseDescriptor + NoteEvent + AccompanimentPreset + ACCOMPANIMENT_PRESETS
lib/exercises/music.ts        # noteToMidi, midiToNote, midiToHz, triad voicing
lib/exercises/engine.ts       # WarmupEngine: descriptor → KeyIteration[] → NoteEvent[]; resolveAccompaniment + buildCue (incl. tonic-hold); PlanInput.octaveShift transposes the whole plan
lib/exercises/library.ts      # imports the 8 JSONs, helpers
lib/exercises/names.ts        # static id→display-name map (for Progress without JSON imports)
lib/audio/player.ts           # AudioPlayer interface
lib/audio/player.web.ts       # Tone.Sampler + Salamander
lib/audio/player.native.ts    # AudioBufferSourceNode + decodeAudioData on bundled MP3s; ±6 semitone pitch-shift
lib/audio/index.{ts,native.ts} # platform resolver
lib/pitch/detector.ts         # PitchDetector interface
lib/pitch/detector.web.ts     # getUserMedia + AnalyserNode + pitchy
lib/pitch/detector.native.ts  # rn-audio-api AudioRecorder.onAudioReady + pitchy
lib/pitch/postprocess.ts      # clarity gate + median filter + octave-jump constraint
lib/scoring/align.ts          # segment → DP-match (octave-aware cost) → per-target snap+score
lib/scoring/score.ts          # Scorer: buffers raw samples per key, finalize() → alignAndScore()
lib/capture/wav.ts            # pure 16-bit PCM WAV encoder (Float32Array → Blob), no deps
lib/capture/types.ts          # CaptureSidecar — sidecar JSON schema for the dev raw-audio corpus
lib/capture/download.ts       # browser file-download helper + filename-safe timestamp (dev raw capture)
lib/capture/songTypes.ts      # SongSidecar — sidecar JSON schema for free-form song recordings (Import → Record)
lib/songs/types.ts            # StoredSong (incl. optional `lyrics?: string`), ChunkSpec, TimeSignature, parseChunkId/buildChunkId for the synthetic id format `song-${songId}__chunk-${chunkId}`. Internal type names kept ("ChunkSpec", "chunks") — user-visible strings say "Segment".
lib/songs/store.ts            # AsyncStorage CRUD at `vocal-training:songs:v1` (mirrors userStore.ts)
lib/songs/chunker.ts          # autoChunk(notes, timeSig, opts?) + reconcileChunkIds(old, new). Default name "Segment N". Cuts at PHRASE_REST_MS=800ms gaps when running chunk has ≥ MIN_CHUNK_BEATS=2 beats, AND at the target-measure tick (snapping backward to a nearby rest end when one's within REST_SNAP_LOOKBACK_BEATS).
lib/songs/syllables.ts        # parseSyllables(text) splits on whitespace, treats hyphens as multi-syllable, strips trailing punctuation. zipSyllablesToNotes(syllables, notes) → { notes, originalIndexMap } — repeatedly bisects the longest single-syllable note (stable, earliest wins ties) until syllableCount === noteCount; pads trailing notes with `""` when fewer syllables; identity map when lyrics are empty.
lib/songs/toDescriptor.ts     # chunkToDescriptor(song, chunk) → synthetic ExerciseDescriptor. When song.lyrics is set, runs allNotes through zipSyllablesToNotes and slices the matched array by mapping originalIndexMap into the chunk's range. Emits restsAfter[] (one entry per scale degree, silence-in-beats AFTER each note). No longer folds gaps into durations[i]. voicePartRanges = { lowest=highest=tonic, step=1 }
lib/songs/preview.ts          # melodyOnlyDescriptor(song, chunk) + previewChunk(player, song, chunk) → SequenceHandle. Strips accompaniment + cue + lead-in clicks for bare-melody segment playback in the song editor
lib/exercises/displayName.ts  # resolveExerciseName(id) — async, looks up chunk-id labels via song store; exerciseNameSync stays for hot paths
lib/progress/routine.ts        # pruneRoutineExerciseIds(predicate) added so song deletion can strip orphaned chunk IDs from RoutineConfig
app/song-editor.tsx            # /song-editor?songId=... route. Loads StoredSong, renders multi-line score with web in-score divider drag + click-to-rename, per-segment ▶/■ preview button, side panel of ◀/▶ nudge controls, multi-line Lyrics textarea (parsed via parseSyllables/zipSyllablesToNotes to drive same-pitch note splitting + per-note syllable annotations). Holds the preview AudioPlayer + SequenceHandle refs; 100ms progress poll clears state on completion. Save calls reconcileChunkIds + saveSong.
components/songs/SongScoreView.tsx       # multi-line wrapped SVG staff with REAL note durations (whole/half/quarter/8th/16th — Bravura noteheads + stems + flags) + explicit Bravura rest glyphs between notes when restAfterBeats ≥ 1/16. Greedy width-based row wrap (replaces fixed notesPerRow). Vertical segment dividers + segment-N labels above each chunk start. Web-only overlay layer: pointer-drag divider <div>s (snap to nearest note column → onBoundaryDragMove) + click-target <div>s on labels that swap to an absolutely-positioned TextInput → onLabelRename. NOT a reuse of MelodyDisplay (single-row infinite scroll). Exports CHUNK_PALETTE so controls can match colors.
components/songs/ChunkDividerControls.tsx # one row per interior boundary with ◀/▶ nudge buttons; disabled when the neighbor would shrink below 1 note. Fallback for native (no drag) + accessibility
components/songs/ChunkNameField.tsx      # inline text input bound to chunk name; commits on blur or Enter. Used both in the segment list row (always) and as the in-score rename TextInput when overlay rename is active
lib/progress/{types,storage,stats,index}.ts  # SessionRecord persistence + summaries
lib/session/tracker.ts        # SessionTracker: routes samples → per-key Scorers
lib/coaching/diagnose.ts      # diagnoseSession: most-consistent or most-glaring mistake from a SessionRecord
components/SyllableDisplay.tsx           # karaoke-style syllable strip (font sizes bumped one Typography tier 2026-05-09)
components/practice/MelodyDisplay.tsx    # SVG staff (BravuraText SMuFL clefs + key sigs) + noteheads aligned with syllables in shared columns
lib/music/keySignature.ts                # major-key signature derivation (circle of fifths) + per-note spelling/accidental decision; 28 tests
assets/fonts/BravuraText.{otf,woff2}     # SMuFL music notation font (SIL OFL); WOFF2 for web (Chrome rejects Bravura's OTF), OTF for iOS native
metro.config.js                          # extends Expo defaults to register woff2 as a Metro asset extension
components/practice/                     # GuidedSession, CoachingBanner, TuningMeter, NoteChip, etc.
components/practice/TodayRoutineCard.tsx # routine card. compact-collapsed: "TODAY'S ROUTINE" eyebrow + status + next-up line + dot bar + "Show all ⌄" pill + Edit. compact-expanded: header (Edit + collapse-chevron) + per-item rows (chevron-right when onItemPress) + "Edit routine — add or swap exercises" footer. non-compact (Progress): header (Edit) + rows. Practice's Edit deep-links to /progress?editRoutine=1, which auto-opens the editor modal (param then cleared via router.setParams).
app/(tabs)/_layout.tsx        # Tab bar — dark "brass bookmark": bgEmphasis (#2c2118) bar, active tab amber (accentOnEmphasis) on a faint amber pill, inactive dim-cream (textOnEmphasisDim). Custom TabBarButton wraps each tab in the pill + carries the iOS haptic. Responsive position: `tabBarPosition` = 'top' when window width ≥768 px (desktop/iPad), 'bottom' below — react-navigation draws the hairline divider on the correct edge and adds the matching safe-area inset, so the explicit height is content-height + that inset.
app/(tabs)/index.tsx          # Practice screen. Desktop ≥1024px: command console (Start + starting-key + Reset + Exercise + Voice + Session Settings, all in one card) on the LEFT, staff card on the RIGHT, one row. <1024px: stacked. Pickers+settings live in the shared <PracticeControls> component.
app/(tabs)/coaching.tsx       # Coaching screen — diagnosis, your-version playback, retry, multi-mistake iteration
app/(tabs)/progress.tsx       # Progress tab (route /progress) — weekly summary, exercise list, recent sessions
components/FeedbackButton.tsx  # Floating bottom-right "Feedback" pill, mounted once in app/_layout.tsx over every screen; opens the Google Form via Linking.openURL. Lifts above the bottom tab bar on <768px layouts.

# First-run onboarding (shipped 2026-06-20)
lib/settings/onboarding.ts               # ONBOARDING_STORAGE_KEY (vocal-training:onboarding:v1) + hasSeenOnboarding/markOnboardingSeen/resetOnboarding. Any non-null value = seen; read fails safe to "seen".
hooks/use-reduced-motion.ts              # web prefers-reduced-motion signal (matchMedia); native falls back to false
app/onboarding.tsx                       # /onboarding route. Owns step index + voice/routine state seeded from app defaults (persisted on change via effects), 6-step list, finish() = markOnboardingSeen + router.replace("/"). app/_layout.tsx gates first-run here (splash held until fonts+flag resolve; latch + canvas cover prevent Practice flash; Practice's HeadphonesBanner is now isFocused-gated so its Modal portal can't bleed over onboarding).
components/onboarding/OnboardingScaffold.tsx   # shared chrome: step dots (DESIGN.md vocab) + always-visible "Skip to singing" + Back/Next bar; per-step entrance motion (reduced-motion aware)
components/onboarding/DemoPlayer.tsx           # useDemoDriver(mode, noteCount, active) — non-interactive timer stepping currentIndex/noteProgress/focusNoteIndex for the live demo; holds a static frame under reduced motion
components/onboarding/steps/*.tsx              # Welcome, VoiceStep (saveVoicePart), RoutineStep (saveRoutine via routineBuiltinItems()), ModeIntroStep (live MelodyDisplay Standard-vs-Guided demo), ImportIntroStep + SongSegmentIntroStep (token-built illustrative cards, not screenshots)
# lib/exercises/library.ts adds routineBuiltinItems() — shared {id,label}[] of built-ins so onboarding + Progress editor can't drift.

# Test infrastructure (PRs 1–2 of automated-testing slice, 2026-05-09 → 2026-05-10)
test/setup-component.ts       # installFakeAudio() + installFakePitch() helpers; Reanimated/AsyncStorage/Haptics/Tone/@expo/vector-icons mocks (vector-icons mocked because MaterialIcons calls Font.loadAsync on mount → blows up in jsdom)
test/mocks/tone.ts            # no-op Tone stub used by component project
test/fixtures/pitchSamples.ts # synthetic PitchSample[] — inTune/flat/sharp/octaveOff/falseStart/silence presets
test/fixtures/keyIterations.ts # buildKeyIterations() calls real WarmupEngine.plan() so fixtures stay schema-aligned
test/fixtures/sessions.ts     # seedSessionRecord() + inTuneFiveNoteSession() builders
jest.config.js                # projects: [unit (ts-jest, Node), component (jest-expo/web, jsdom)]; coverageThreshold gates 5 PR-2 files
babel.config.js               # babel-preset-expo for jest-expo (and Metro)
# PR 2 unit suites (added 2026-05-10):
lib/scoring/__tests__/align.test.ts        # 28 tests — segment/filter/match/alignAndScore
lib/pitch/__tests__/postprocess.test.ts    # 14 tests — clarity gate, median filter, octave-jump constraint
lib/session/__tests__/tracker.test.ts      # 9 tests — sample routing, key boundaries, snapshots
lib/progress/__tests__/stats.test.ts       # 15 tests — summarize/progress/thisWeek/bestKey/bestSession/rolling
lib/exercises/__tests__/music.test.ts      # 18 tests — noteToMidi/midiToNote round-trip, durations, triads, voicing
# PR 3 integration suite (added 2026-05-10):
lib/__tests__/integration/engineIntegration.test.ts  # 5 fixture-driven scenarios end-to-end (Tracker → Scorer → align → diagnose)
# Song-import slice E tests (added 2026-05-30):
lib/exercises/__tests__/engine.rests.test.ts   # 5 tests — descriptor restsAfter shifts onsets, emits "rest" NoteEvent, defensive length mismatch, scorer-target filter unaffected
# PR 4 component-test scaffolding (added 2026-05-10):
app/__tests__/coaching.test.tsx           # 3 cases — flat-session diagnosis + bookmark persistence + error states
app/__tests__/progress.test.tsx           # 3 cases — weekly summary + recent-session "Coach this" router.push
app/__tests__/practice.test.tsx           # describe.skip — Practice deferred until UX audits stabilize the screen
.github/workflows/test.yml                # typecheck + jest CI (Playwright e2e lands in PR 5)
```

**Data flow per sample:**
PitchDetector → postprocessor (clarity/median/octave gate) → screen RMS gate (`-45 dB`) → SessionTracker → per-key Scorer → snapshot → React state

**Audio flow per exercise:**
ExerciseDescriptor + voicePart → `planExercise()` → `KeyIteration[]` → `flattenIterations()` → `NoteEvent[]` → `AudioPlayer.playSequence()` → Tone.Transport.scheduleOnce (cancellable)

## What works

- **Usability & hardening sprint** (shipped 2026-06-23, plan at `USABILITY_HARDENING_PLAN.md`; web-only/local-first/no-external-resources scope): (A1) data durability — `requestPersistentStorage()` on boot (`lib/storage/persist.ts`) + all-data JSON export/import (`lib/backup/exportImport.ts`, single-source `BACKUP_KEYS`) + "YOUR DATA" backup/restore footer + >30-day nudge on Progress; **fully local, no cloud**. (A2) honest scoring — `NoteScore.octaveBelow`/`octaveAbove` flag octave-folds; `PostSessionPanel` shows a non-judgmental "sang an octave below" hint linking to the "Down an octave" selector. (A3) app-wide `components/ErrorBoundary.tsx`. (B1) first-class mic-failure recovery — `handleStart` preflights mic acquisition *before* the piano; on failure `lib/pitch/micError.ts` → `MicErrorState` renders in the staff card with per-browser steps + Retry; `MicLevelMeter` (fed by `PitchSample.rmsDb`) under Start. (B2) desktop right-column "LAST TIME" panel + `currentStreak` + one-line "what this trains" — **above-the-fold-safe** (Start never pushed down). (B3) warm Progress/Coaching empty states + decoded "Piano/Guide/Demo" settings labels. (C1) personal-best badge (`isPersonalBest` in `stats.ts`) + spacebar Start/Stop + → next. **Deferred:** last-key-degradation fix (needs repro). **Shipped 2026-06-23:** web piano now bundled in `public/salamander/` (full 21-sample set, served same-origin) — no `tonejs.github.io` CDN dependency.
- **Per-exercise pitch-detection disable + follow-along** (shipped 2026-06-23): `ExerciseDescriptor.pitchDetection?: boolean` (default true). When `false` (set on `rossini-lip-trill` — lip buzz defeats pitch detection), Practice runs an **unscored follow-along**: no mic/preflight/scoring/`MicErrorState`, no Guided toggle, no level meter, headphones gate bypassed, a "Follow along — …isn't scored. Match the piano." banner, and a completion card with "Mark done" (logs a `keyAttempts:[]` record so the routine checks off) / "Skip". `lib/progress/stats.ts` excludes `keyAttempts.length === 0` sessions from accuracy/best/trend aggregations (still counted as sessions) so follow-alongs don't drag stats. Also: **`goog-octave-arpeggio` tempo 150→90** (8n note 0.20s→0.333s) so it crosses `resolveDetectorTuning`'s 0.30s sustained-profile threshold; dropped its `octaveJumpFrames:2` hint; engine regression snapshot refreshed.
- **First-run onboarding** (`/onboarding`, shipped 2026-06-20): shows once, gated on `vocal-training:onboarding:v1`. Always-visible "Skip to singing" (skip = done forever; safe defaults). Steps: Welcome → Voice (`saveVoicePart`) → Routine (`saveRoutine`, seeded from `DEFAULT_ROUTINE`) → live Standard-vs-Guided demo (real `MelodyDisplay`, no audio) → Import + Song-segment illustrative intros. Gated in `app/_layout.tsx` (splash held until fonts+flag resolve, latch + cover prevent Practice flash; Practice's headphones Modal is `isFocused`-gated so it can't bleed over onboarding). Honors reduced motion. Dev reset in Trill Lab. See the onboarding architecture block above.
- Practice screen: pick exercise + voice part (soprano/alto/tenor/baritone) + Standard/Guided mode. **Desktop ≥1024 px = command-console layout**: a single console card on the LEFT holds the big Start/Stop button, the starting-key label + Reset, the Exercise picker, the Voice picker, and the Session-settings icon cluster (accompaniment / guidance / demo) — staff card on the RIGHT, one horizontal row. <1024 px: everything stacks (staff → Start bar → pickers → settings). The pickers + settings live in the shared `<PracticeControls>` component (used by the console on desktop, stacked below the staff on mobile, and below `GuidedSession` in Guided mode). When a session is running the console swaps to Stop + key/note counter + progress bar — there is intentionally **no live pitch readout** (real-time detection octave-errors enough to look wildly off when it isn't; scoring is post-pattern). The active exercise auto-tracks the routine's next not-yet-done item (on app open and after each logged session); it backs off once the user explicitly picks one.
- Per-exercise tonic memory: each `(exerciseId, voicePart)` pair remembers its last-reached tonic; RESET button above the note chips snaps current exercise back to range lowest
- **Octave selector** (Practice controls): "Notated" / "Down an octave" — per-session, persisted at `vocal-training:octave-shift:v1`. Transposes the whole exercise via the engine's `octaveShift` (piano, staff, syllables, scoring all move together) so a singer who sings below the notation practices in their own register instead of leaning on octave-correction to silently fold it. Threaded through Standard + Guided; stored on `SessionRecord.octaveShift` for correct Coaching replay. Phase 1 of the octave-mismatch fix (Phase 2: surface octave drift instead of folding it).
- Headphones modal: blocks Start until the user confirms once per app session; "Continue without" biases the RMS gate up by 6 dB
- Per-note breakdown chip strip under each completed-key row in Standard mode (shared `<NoteResultsStrip>` component); empty windows render `—` not `0¢`
- Tone.js + hosted Salamander samples on web; bundled Salamander MP3s + `AudioBufferSourceNode` on iOS (code in place; awaiting on-device validation)
- Cue types (`ding` / `block` / `bell` / `v7` / `tonic-hold`) honored at runtime; descriptors with `lockAccompaniment: true` (Rossini lip trill, Ng siren) ignore preset overrides
- Lead-in: 2 audible click-track ticks between cue and melody on every preset except Classical (which already cues entry via V7 → I)
- Live pitch detection via getUserMedia (web) / `AudioRecorder.onAudioReady` (native) + pitchy with PRD-mandated gating (clarity, median filter, octave-jump hold — per-exercise override via `scoringHints.octaveJumpFrames`)
- **Pattern-alignment scoring** (replaced onset-detection on 2026-05-05): samples buffer per key during the pattern; on key end, `lib/scoring/align.ts` detects N stable pitch segments (≥75¢ stable, ≥80 ms duration), filters false starts, then DP-matches segments to expected targets (Needleman-Wunsch with 6-semitone gap penalty). Scoring is musical-context-driven, not time-window-driven — eliminates per-exercise latency calibration sensitivity.
- **Dynamic RMS gate**: `rmsGateFor(preset, headphonesConfirmed)` returns −45 to −33 dB depending on whether the active preset doubles the melody and whether the user confirmed headphones
- Karaoke syllable strip (active scaled 2× and underlined), per-key summaries + per-note chip strips (Standard) — but no real-time pitch readout / tuning meter on the Practice screen (deliberately removed; see above)
- **Guided mode**: hold-and-match per-note with configurable threshold (±25/50/75/100¢). On pattern complete, a per-note results breakdown shows the best cents achieved on each syllable in the pattern (color-coded vs threshold) — review surface, not live feedback.
- **Coaching screen** (`coaching.tsx`): diagnoses the most-glaring or most-consistent mistake, plays "Correct version" / "Your version" (Hz substitution into the sequence), single-key retry with live scoring, "Find next mistake" iteration, child-session persistence
- **Progress tab** (`progress.tsx`, route `/progress`): Today's routine card at top (configurable, 4 defaults, edit modal) → weekly summary card → per-exercise list (last-practiced + best key + best-ever% + tap-to-expand sparkline trend) → recent sessions list (last 20, each tap-to-expand into per-note breakdown chips + "Coach this" button that loads the historical session into coaching via `?sessionId=` route param). Vanilla View-based sparkline; no chart library. Empty state for no data.
- **Opt-in logging** for Standard sessions: "Log session" with optional note / "Discard" UI after each session ends. Coaching child sessions still auto-save.
- Session persistence: SessionRecord at `vocal-training:sessions:v1` (AsyncStorage); coaching child sessions linked via `parentSessionId`. Routine config at `vocal-training:routine:v1`.
- Stop button cancels pending audio events on both platforms
- RMS noise gate (dynamic, see above) prevents piano-spillover from poisoning scores
- **Octave-error tolerance lives in `lib/scoring/align.ts`, not the scorer** (changed 2026-05-11). pitchy's MPM latches onto sub-harmonics on chesty notes (~100–250 Hz), reporting e.g. G3 as G2 — exposed by the 2026-05-09 tenor range bump. Two pieces: (1) `matchToTargets()`'s DP cost (`matchCost`) is octave-aware — a segment an exact octave off its target matches at a small penalty (1.5 st, below the 6-st skip penalty) instead of the full ~12, only if the post-octave-fold residual ≤ 2 st (so a genuine wrong note isn't folded); (2) `snapOctave(rawMidi, targetMidi)` rounds `rawMidi` to the nearest octave of the target (any ±12k), keeping it only if it lands within ±2 st. The old per-sample pre-snap in `Scorer.append` (which snapped against the *globally nearest* target, computed on the already-errored pitch — wrong reference) was removed; `Scorer` now buffers raw samples and `alignAndScore` does all octave correction against each segment's *matched* target.
- **Staff notation with proper SMuFL clefs + key signatures** above the syllable strip on Practice / Guided / Coaching surfaces (`components/practice/MelodyDisplay.tsx`). SVG-based via `react-native-svg`. Treble vs bass clef chosen by mean MIDI of the displayed melody. Real treble (U+E050) and bass (U+E062) glyphs from BravuraText (SMuFL music font, SIL OFL). Per-key signature derived via circle of fifths from `lib/music/keySignature.ts`; on-note accidentals appear only when the note breaks from the key signature (chromatic alteration → ♯/♭; in-key letter that's enharmonic to a natural → ♮).
- **DI registry pattern** at `lib/audio/index.{ts,native.ts}` and `lib/pitch/index.{ts,native.ts}`: factories can be swapped per-test via `__set/__resetAudioPlayerFactory` and `__set/__resetPitchDetectorFactory` (call sites in feature code unchanged).
- **Dev-only raw-audio capture** (Slice 1 of the pitch-eval pipeline, web-first): a `__DEV__`-gated "Record raw audio" toggle in the Practice settings cluster. When on, `PitchDetector.enableRawCapture()` (called before `start()`) taps the web detector's mic source with a continuous `ScriptProcessorNode` that accumulates every Float32 sample gap-free (separate from the gappy RAF analyser read). On session end (`handleStop`, covers both natural completion and manual Stop), `getRawCapture()` returns the concatenated PCM; the screen encodes a 16-bit mono WAV (`lib/capture/wav.ts`) and a `CaptureSidecar` JSON (`lib/capture/types.ts` — exerciseId/voicePart/startTonic/tempo/sampleRate/durationMs/`audioStartOffsetMs`/`keyStarts`/`expectedTargets`), and triggers two browser downloads named `<exerciseId>__<voicePart>__<tonic>__<timestamp>.{wav,json}`. Capture is purely additive — disabled = byte-identical live detection. Native gets no-op stubs (`enableRawCapture`/`getRawCapture` are optional on the interface so test fakes needn't implement them). Corpus lands in `test/fixtures/audio/corpus/` (gitignored except its README).
- **In-app song recording** (Import modal → **Record** tab, `components/import/RecordingForm.tsx`): reuses the same mic-tap primitives (`createPitchDetector` + `enableRawCapture`, `encodeWav`, `downloadBlob`). User sets song name + key center + mode + voice part, taps Start, sings the melody without backing music, taps Stop; the WAV is handed straight to the existing `analyzeFile` → review → save-as-user-exercise flow. In `__DEV__` (web) the take is also dropped to `test/fixtures/audio/songs/` (gitignored; `SongSidecar` schema in `lib/capture/songTypes.ts`) for offline iteration on extraction techniques.
- **Automated test pyramid PRs 1–4 shipped**: Jest projects split (unit + component), full fixture infrastructure, smoke tests proving the registry works (PR 1). PR 2 added 86 unit tests for the 5 critical pure-TS files (`lib/scoring/align.ts`, `lib/pitch/postprocess.ts`, `lib/session/tracker.ts`, `lib/exercises/music.ts`, `lib/progress/stats.ts`) with `coverageThreshold` gates. PR 3 added 5 engine integration scenarios at `lib/__tests__/integration/engineIntegration.test.ts` that drive synthetic `PitchSample[]` through the real `SessionTracker → Scorer → alignAndScore → diagnoseSession` pipeline. PR 4 shipped scaffolding-only component-test infrastructure (`@testing-library/react` for DOM queries against jest-expo/web's react-native-web output; expo-router mock + `window.matchMedia` polyfill in `test/setup-component.ts`) plus two real screen tests at `app/__tests__/coaching.test.tsx` and `app/__tests__/explore.test.tsx`; Practice deferred via `describe.skip` until the UX audits stabilize the screen. CI lives at `.github/workflows/test.yml` (typecheck + jest jobs). **384 tests / 31 suites / 2 projects passing (1 skipped)** (incl. 3 octave-tolerance regression tests in `align.test.ts` added 2026-05-11). Verify coverage gates with `npm run test:coverage` (scoped to the unit project — running both projects pulls postprocess.ts coverage down because the component project loads it without exercising methods). PR 5 (Playwright E2E + WAV fixture pipeline) is still open, gated on whether the user wants the full Practice happy path covered before UX has hardened.
- **Voice-range validation** (`lib/music/voiceRanges.ts`): `VOICE_RANGES` table (lowest, highest, passaggio per voice part — Miller / Doscher / Sundberg pedagogy). `validateDescriptorRanges()` checks each exercise's voice-part rows for off-by-octave bugs, copy-paste between voice parts, and whether the highest tonic iteration actually crosses the voice's passaggio (with SOVT exemption for lip-trills/sirens). Library audit asserts every shipped descriptor passes; sanity checks include "tenor and baritone are not byte-identical" and "tenor lowest > baritone lowest". `clampTonicToVoiceRange()` is the extracted testable form of the screen's tonic-memory clamping logic. 17 tests in `lib/music/__tests__/voiceRanges.test.ts`.
- **Audited + bumped ranges across all 8 exercises (2026-05-09):** Original descriptors had tenor parts an octave below typical warmup territory (e.g. five-note-scale tenor C3-G3 peaked at D4 — sounded like a baritone exercise). Final ranges peak at typical tenor warmup levels: narrow-span exercises (5-note scale, nay 1-3-5-3-1, descending 5-1) tenor G3-F4 → peaks D4-C5; octave-span exercises (Goog, octave-leap, ng-siren, staccato) tenor D3-A3 → peaks D4-A4; rossini-lip-trill bounded for daily-warmup comfort at tenor C3-Eb3 → peaks G4-Bb4 (lowered 2026-05-14 from G3 cap which peaked D5; SOVT span is octave+fifth so top tonic dominates the ceiling). Baritone parts shifted up symmetrically. Validator gates against off-by-octave regressions (passaggio + 2 minimum; SOVT-relaxed). Engine regression snapshots refreshed.
- **Alto + soprano voice parts added (2026-05-09):** all 8 exercises now define ranges for soprano, alto, tenor, baritone. Voice picker in `app/(tabs)/index.tsx` shows all four chips high-to-low. Audit tests enforce: (a) every descriptor defines all 4 parts; (b) no two voice-part ranges within a descriptor are byte-identical; (c) ranges follow soprano > alto > tenor > baritone by lowest tonic; (d) snapshot of out-of-native-cap peaks (8 soprano peaks at A5 = MIDI 81 are above the F#5 native pitch-shift cap — works fine on web, will produce native artifacts until a higher Salamander sample is bundled in `assets/salamander/`).
- **Optional lyrics on imported songs (Slice F):** the song editor exposes a free-form Lyrics text field. Parsing splits on whitespace, treats hyphens as multi-syllable (`for-ev-er` → 3), strips trailing punctuation. When the parsed syllable count exceeds the analyzed-note count (singer held one pitch across several syllables) the longest single-syllable note bisects until counts match — the only honest signal that "love you forever" on one sustained note should be 3 notes. Lyrics are stored on `StoredSong.lyrics`; `chunkToDescriptor` applies the split before slicing chunk ranges (using the syllables module's `originalIndexMap` so chunk boundaries stay aligned). Score views render syllables under each notehead — `SongScoreView` adds an `SvgText` below each notehead, `VexFlowScore` attaches `Annotation` modifiers (BOTTOM justify). Practice / Guided / Coaching karaoke strips pick up the new syllables automatically via `descriptor.syllables`.
- **Head Voice Vwohm exercise (2026-06-12):** ninth built-in (`data/exercises/head-voice-vwohm.json`), user-defined (not from the PRD). Two descending two-note slides — degrees 6→4, quarter rest, 4→1 — sung "vwo"/"ohm", quarter-then-dotted-quarter rhythm at 72 bpm, sustained-chord accompaniment + bell cue, direction ascending (leads head voice up into the challenging range). First built-in descriptor to use per-note `durations` + `restsAfter` (previously song-chunk-only paths); `engine.regression.test.ts`'s parity suite now filters to duration-less descriptors and its snapshot describe was renamed count-free ("locks every bundled descriptor") so future additions don't re-key snapshots.
- **Bub Mix Voice exercise (2026-06-16):** tenth built-in (`data/exercises/bub-mix-voice.json`), user-defined (not from the PRD). Descending major arpeggio 8-5-3-1 (scaleDegrees `[12, 7, 4, 0]`) sung "bub" on every note, quarter notes at 100 bpm, block-chord accompaniment + bell cue, direction both (keys start at the range bottom, climb a half step per iteration up through the passaggio, then descend back down). Reuses the goog/staccato octave-span ranges (soprano C4-A4 … baritone C3-F#3); soprano peaks A5 = MIDI 81 (above the native F#5 cap, locked in the `voiceRanges.test.ts` snapshot like the other soprano configs). No `durations`/`restsAfter` — uniform `noteValue`.
- **Capability taxonomy + growth paths** (shipped 2026-06-23, from `EXERCISE_TAXONOMY_RESEARCH.md`): `lib/exercises/capabilities.ts` defines 6 capability categories (`warmup-sovt`, `onset`, `resonance`, `mix`, `range-passaggio`, `agility`) each with a label + "what this builds" blurb, ordered foundation→advanced. `ExerciseDescriptor.capability` tags every built-in; `groupByCapability(list)` (in `library.ts`) buckets any exercise list in display order (uncategorized imports/song-chunks → trailing "Your imports"). The **Practice exercise picker is regrouped by capability** with eyebrow label + blurb per group + a capability badge on the selected exercise (songs + "+ Import" preserved below). `lib/exercises/pathways.ts` defines 4 curated, **ungated** growth paths (Daily Foundations / Build Your Mix / Open Your Range / Agility & Release) — ordered exercise lists, `pathwayProgress(path, everPracticed, doneToday)`. `components/practice/PathwaysCard.tsx` on Progress shows each path's focus, description, a practiced/total progress bar + done-today count, expands to its exercises (tap → Practice deep-link), and "Practice this path" → `saveRoutine` (the path becomes the routine, tracked by the existing streak/completion system). **No mastery-gating** — deferred until aggregate data can stratify it. Audit: `lib/exercises/__tests__/capabilities.test.ts`.
- **Plan tab + coming-soon categories** (shipped 2026-06-24): third top-level tab `app/(tabs)/plan.tsx` (between Practice and Progress; clipboard icon) = the "set up your practice" surface — (1) **Browse exercises** by capability (all 9 `CAPABILITIES` in order; tap an exercise → deep-link to Practice via `?exerciseId=`), (2) **Today's routine** card + Edit (opens the now-shared `components/practice/RoutineEditModal.tsx` — extracted from `progress.tsx` along with `buildRoutineItems()` so both surfaces build identical built-in+user+song-chunk item lists), (3) **Bring your own** → `ImportModal`. `capabilities.ts` gained 3 `comingSoon: true` categories (`dynamics`, `vibrato`, `style`) — acknowledged CCM techniques with no trusted exercises yet; they render as "Exercises coming soon" on Plan's browse (and are dropped by `groupByCapability`, so the Practice picker stays clean). `isComingSoon(id)` helper.
- **Belt/Vibrato/Dynamics exercises** (shipped 2026-06-24, library 15 → 20; from `BELT_VIBRATO_DYNAMICS_RESEARCH.md`): the 3 formerly-coming-soon categories are now populated, so `comingSoon` was removed from all three. **Belt & Style** (3, scorable, from Roll 2016 J.Voice): `belt-arpeggio-mah` ([0,4,7,4,0] "mah", Overdrive), `belt-nyah-descending` ([7,5,4,2,0] "nyah", Edge/twang), `high-belt-wee` ([7,4,0] "wee", high-belt closed vowel). **Vibrato** + **Dynamics** are single-note `durations` sustains run as **follow-along** (`pitchDetection:false`) because mean-cents scoring can't grade vibrato regularity or loudness: `straight-tone-vibrato` ([0] held 6 beats) and `messa-di-voce` ([0] held 8 beats). Single-note sustains can't satisfy the validator's passaggio-crossing rule, so `voiceRanges.test.ts` adds a `SUSTAIN_EXERCISE_IDS` relaxation (keyed by ID, like SOVT). New pathways: Belt & Power, Control & Expression. The follow-along banner copy is now generic (was lip-trill-specific). Engine-parity test now allows 3 durations-declaring exercises.
- **Register taxonomy restructure + Vocal Habit rebrand** (shipped 2026-06-24, library 20 → 22): the vague `range-passaggio` capability was renamed → `head-voice` (its 5 exercises retagged), and a new `chest-voice` category added so the register lane reads **Chest → Mix → Head** (how singers actually think). Two chest exercises authored (`chest-voice-mum` [0,4,7,4,0], `chest-descent-mah` [7,5,4,2,0], both scorable, lower goog-style ranges) — they sit below the passaggio by design, so `voiceRanges.test.ts` adds a `CHEST_EXERCISE_IDS` relaxation alongside SUSTAIN/SOVT. New pathway `ground-your-chest`. Also: **Plan browse** exercises are now rows with explicit `Add to routine`/`✓ In routine` toggle + `Practice ▶` (was a chip that silently deep-linked); **onboarding RoutineStep** renamed "warmups"→"exercises", grouped by capability with blurbs + "edit later" reassurance. **Rebrand:** app renamed "Vocal Training" → **Vocal Habit** (`app.json` name — build-time, applies on deploy; slug stays `vocal-training` for the Netlify subdomain), Practice `<h1>` "Vocal Warm-up" → "Vocal Habit", and the index route's document title = "Vocal Habit" with `tabBarLabel` still "Practice". Domain: **vocalhabit.com** (Namecheap ALIAS → Netlify).
- **Tempo-aware pitch-detector tuning (2026-05-24):** `lib/pitch/tuning.ts` `resolveDetectorTuning({ noteSec, hints })` returns `{ clarityThreshold, smoothingFrames, octaveJumpFrames }` from per-note seconds — sustained defaults (0.85 / 5 / 3) for `noteSec >= 0.30 s`, a fast-note profile (0.65 / 5 / 1) below. Used by both `lib/analyze/framewise.ts` (`runOfflinePitch` accepts `tempoSec` + `scoringHints`; the eval harness `scripts/eval-corpus.ts` passes them) and the live Practice screen `handleStart` (replaces the old static `setClarityThreshold(0.85) / setOctaveJumpFrames(3)` lines). Explicit `scoringHints` overrides always win. Validated on the eval corpus: goog-octave-arpeggio (noteSec 0.20) coverage 75/80 → 87/91 %, RPA 71/76 → 81/85 %; the 5 normal exercises (noteSec ≥ 0.34 s) are byte-identical; lip-trill (out-of-scope class apart) is not regressed. Live detector can only swap clarity + octave-jump at runtime (the postprocessor's median window is constructor-set); the resolver picks the same median value for both branches, so the live path matches the offline resolution exactly on existing descriptors.

## Web rendering & SEO (Option A: Expo static export)

**Decision (2026-06-24):** migrate `web.output` from `"single"` (pure SPA) → `"static"` (SSG) so marketing / Learn / tool pages are indexable. Driven by `seo/keyword-research-2026-06.md` — the category is winnable (top targets `vocal range test`, `vocal warm ups` are low-difficulty; those SERPs reward interactive tools, which is the app's native strength). Site is brand new (DR 0); benchmark competitor Singing Carrots is only DR 33 / ~24k organic visits/mo. Planning docs in `seo/`:
- `seo/static-rendering-architecture.md` — foundational eng work (root-layout de-gating, route/group structure, per-route head/meta, hydration, build + Netlify deploy, QA)
- `seo/learning-library-plan.md` — the Learn section (articles ↔ keyword clusters, embedded-exercise pattern, internal linking, rollout)
- `seo/range-tester-plan.md` — the `/vocal-range-test` standalone tool page (SHIPPED — see below)
- **`seo/content-style-guide.md` — READ BEFORE WRITING ANY article / tool page / marketing copy.** The verbal counterpart to DESIGN.md: "Jeff Nippard for singing" voice (well-informed, science-backed, non-dogmatic, surfaces real disagreements), plus the MANDATORY adversarial fact-check process (≥2 refutation-oriented lenses, verified sources only, never fabricate citations, health claims must be current). Per-page verdicts/sources go in a `*-content-sources.md`.

**Shipped (branch `feat/static-rendering-migration`):** the **Vocal Range Test** page — `app/(marketing)/vocal-range-test.tsx` (static SEO content + JSON-LD via `expo-router/head` child-string `<script>`), `components/tools/RangeTesterIsland.tsx` (interactive island: mic → sustained-pitch capture → voice-type classification; lazy-loads the engine), `lib/tools/rangeTest.ts` (pure + 16 unit tests). Content was adversarially fact-checked — verdicts in `seo/range-tester-content-sources.md`. JSON-LD note: only the child-string `<script type="application/ld+json">{JSON.stringify(x)}</script>` form flushes to static HTML; `dangerouslySetInnerHTML` does not.

**Learn hub — ONE surface, two routes (don't re-fork).** The Learn hub content (intro + Vocal Range Test card + `<SpotlightCarousel/>` + search + category chips + article grid) lives in a single shared component `components/learn/LearnHub.tsx`. It is rendered by BOTH the static SEO route `app/(marketing)/learn/index.tsx` (`/learn`, adds the `<Head>`) AND the in-app "Learn" tab `app/(tabs)/library.tsx` (route `/library`, labeled "Learn" in `app/(tabs)/_layout.tsx`). They must stay a shared component — a 2026-07-06 bug had `/library` as a separate, poorer list missing the spotlight carousel, so the in-app tab and the marketing page silently diverged. Edit `LearnHub`, never a copy. The route split is forced: `/learn` must be a static (marketing-group) SSG page for SEO, while the tab must live in `(tabs)` to keep the tab bar; `/library` keeps that path because `(marketing)/learn` owns `/learn`. `LearnHub` must stay SSG-safe (system-font fallback, `Link` not `router.push`, no browser-only APIs).

**Core pattern — static content + hydrated island:** pre-render every ranking element (copy, headings, FAQ, structured data) to real HTML at build time; the interactive piece (mic + pitch detection) hydrates client-side and only activates on a user gesture (mic requires one anyway). The engine is already framework-agnostic TS (`lib/pitch/*`, `lib/scoring/*`, `lib/exercises/music.ts`, `lib/music/voiceRanges.ts`), so SEO pages mount it as an island without pulling in the whole app.

**Constraint + as-built (2026-06-24):** `output: "static"` builds fine, but the app pre-renders EMPTY shells because `app/_layout.tsx` gated the whole tree on client-only state (fonts + AsyncStorage onboarding check) and effects don't run during static generation. **As-built fix (branch `feat/static-rendering-migration`):** the root layout keeps the gate for app routes but **bypasses it for a `STATIC_SEGMENTS` whitelist** (currently `(marketing)`) via `useSegments()` — so routes in `app/(marketing)/` render synchronously to indexable HTML while app routes stay (safely) empty shells and never execute browser-only render paths during SSG. This avoided moving the app into an `(app)` group (which would've broken `@/app/(tabs)/...` test imports). Verified: a `(marketing)` probe route exported real `<body>` content + a populated `<title>`; `index.html` body stayed empty; `tsc` + 669 tests green. The `(marketing)` group was removed after proving it (no real pages yet); recreate it when building the range tester / Learn pages — `STATIC_SEGMENTS` is already wired.

**Rules for SEO routes:**
- Ranking content renders synchronously — must appear with fallback system fonts; custom fonts (Fraunces / General Sans / Bravura) enhance only, never gate.
- Per-route `<head>` (title / meta / canonical / JSON-LD) via `expo-router/head`.
- Lazy-load heavy audio deps (`tone`, `pitchy`, `react-native-audio-api`, Salamander samples) so marketing / Learn routes stay light.
- **Embedded-exercise SEO rule:** article prose / headings / FAQ live in static HTML; the exercise is a hydrated island. Putting substantive copy *inside* the client-only component forfeits the SEO value — that's the empty-shell failure mode.
- Verify rendered output by view-source / `curl` (content present in `dist/<route>.html`), not just devtools.

**Deploy:** `npx expo export --platform web` → `dist/` → Netlify (static host; vocalhabit.com).

## Known limitations / non-yet-done

- **iOS dev build** not yet generated. Needs `npx expo prebuild && npx expo run:ios`. The Salamander player and native pitch detector are coded but unvalidated on real hardware. RMS gate threshold may need per-platform tuning.
- **iOS staff-notation glyph rendering unvalidated.** BravuraText.otf bundles for native, but `react-native-svg`'s `SvgText` with custom `fontFamily` on iOS is unverified. If clefs/accidentals don't render on device, the fallback is to switch to `<Path>` data for the ~5 needed glyphs (treble/bass clef, sharp/flat/natural).
- **Native sample cap of F#5.** 8 of 9 soprano configurations peak at A5 (MIDI 81) — lip-trill now peaks F#5 (at cap, ok). The bundled Salamander samples top out at C5; with ±6 semitone pitch-shift the native player can reach F#5 (MIDI 78) cleanly but artifacts above that. Web is unaffected. Resolution: bundle a C6 Salamander sample in `assets/salamander/`. Locked in `voiceRanges.test.ts` snapshot so silent regressions surface in PR diffs.
- **Lyrics note-splitting is a heuristic** — when syllableCount > noteCount the longest note bisects until counts match. It can't tell *which* held note was actually sung as multiple syllables; for a "love you forever" held on the second note when the first is longer, it would split the first instead. Workarounds: type a placeholder syllable on a comma to nudge the split, or trim the lyric to match the actual analyzed notes.
- **No standalone Library tab** — Progress covers history; a pedagogy-first browser of all 9 exercises (descriptions, PRD references) is still open.
- **No session prune/delete** — AsyncStorage sessions accumulate indefinitely.
- **Web rendering migrating SPA → static export for SEO** — see **§Web rendering & SEO** above. The old `tslib` bug was specific to `output: "server"` (full SSR); `output: "static"` (SSG) exports cleanly. **Migration shipped on branch `feat/static-rendering-migration` (2026-06-24, uncommitted):** `app.json` `web.output: "static"` + root-layout `STATIC_SEGMENTS` de-gating; `tsc` + 669 tests green; render verified. Remaining before this goes live: validate Netlify routing/fallback for the per-route HTML, and add the first real `(marketing)` page (range tester) to actually have indexable content deployed.
- **Octave errors** from pitchy's MPM (sub-harmonic on chesty notes ~100–250 Hz, second-harmonic up high). *Scoring* now tolerates them via `align.ts` (octave-aware DP match + per-target snap, see "What works"), so results are right even when pitchy is an octave off. Two things still rough: (1) the live "YOU" readout shows the raw postprocessor output, so it can read e.g. "G2" while you sing G3; (2) if pitchy's first stable frame on a note is a sub-harmonic, the postprocessor's octave-jump constraint can anchor its median there and suppress the correct octave for the rest of the note. Candidate follow-ups: snap the "YOU" readout against the current target for display; make the postprocessor's octave-jump logic vote by frequency rather than consecutive runs.
- **`.codeyam/`** directory is legacy (we're not using CodeYam). It's gitignored. Safe to `rm -rf` once the local CodeYam server (port 3111) is stopped.

## Test status (PRs 1–4 shipped — 2026-05-09 → 2026-05-10)

`npm test` → 452 tests / 38 suites / 2 projects (1 test + 1 suite skipped) in ~90s. `npm run test:coverage` (scoped to unit project) verifies the configured `coverageThreshold` block. `tsc --noEmit` clean. CI at `.github/workflows/test.yml`.

**Component-test conventions (PR 4):**
- Use `@testing-library/react` (DOM-based) — `jest-expo/web` renders RN through `react-native-web` so the tree is HTML in jsdom; `@testing-library/react-native` v13's react-test-renderer path doesn't see the same nodes.
- Drive `expo-router` via `setMockRouterParams()` / `getMockRouter()` from `test/setup-component.ts`. Reset is automatic in `afterEach`.
- DI fakes via `installFakeAudio()` / `installFakePitch()`. Same auto-reset.
- **UI tests stay deliberately sparse until the UX audits land.** The screens are still in flux — picker chips, settings cluster, modal copy, mode toggles, etc. Tightly-coupled assertions today become next month's churn. Each tested screen file carries a top-of-file `// COMPONENT TEST:` comment naming the test file so edits to rendered text or accessibility labels surface during code review. When you add a new screen test, add the same cross-reference comment.

**What's covered:** the existing pure-TS layers (coaching detectors, exercise planning, session storage, key analysis, melody synthesis, theme constants, key signature theory), 7 smoke tests proving the test infrastructure works (PR 1), 86 unit tests across the 5 PR-2 critical files (`align.ts`, `postprocess.ts`, `tracker.ts`, `stats.ts`, `music.ts`), 5 engine integration scenarios driving the full scoring + diagnosis pipeline (PR 3), and 2 component tests covering Coaching diagnosis + bookmarking and Progress tab navigation (PR 4 at `app/__tests__/coaching.test.tsx` + `app/__tests__/explore.test.tsx`).

**What's NOT covered (by PR):**
- **PR 4 deferred:** Practice screen component tests (`app/__tests__/practice.test.tsx` is a `describe.skip` placeholder with the to-do list embedded; unskip after UX audits).
- **PR 5:** Playwright E2E (5 scenarios) + WAV fixture pipeline + spy on `Tone.Sampler.triggerAttackRelease`.
- **Phase 5 (after Slice A):** Maestro iOS smoke flows (4 nav-only YAMLs).

**Clean-run state (2026-05-11):** `npm test` output is now noise-free — the two `console.warn` lines that used to clog the log (`[storage] Pruned …` from `lib/progress/storage.ts:173`, `[savedStorage] Pruned …` from `lib/coaching/savedStorage.ts:55`) are swallowed in the cap-overflow tests that intentionally trip them (the tests now also assert the warn fired), and the "2 snapshot files obsolete" warning is gone — each Jest project now sets `roots` so it doesn't crawl the other project's `__snapshots__` dirs. The one remaining "1 skipped" is the deliberate `describe.skip` placeholder in `app/__tests__/practice.test.tsx` (Practice screen tests deferred until the UX audits land); a skipped test does not fail the run, so deployment gating should key on failures, not on a zero-skip count. Unrelated environment noise that may still appear depending on the runner: Node's `punycode` DEP0040 warning and an occasional flaky "worker process failed to exit gracefully" from the jsdom component project.

**Pending cleanups:** `@testing-library/jest-native` is deprecated and should be removed when PR 4 lands, `jest-expo@55.0.17` is one minor ahead of SDK 54's expected version (works fine, emits a warning).

The full plan with rationale, slicing, and risk register is at `~/.claude/plans/glistening-wiggling-hamming.md`. Read that before starting any subsequent PR.

## Run

```bash
npm install
npx expo start --web --port 8081     # web dev — http://localhost:8081
# For phone testing over LAN/HTTPS:
ngrok http 8081                       # then open the https URL on phone Safari
```

Mic access requires HTTPS on iOS Safari, hence ngrok for phone-on-web testing. Once iOS dev build is set up, `expo run:ios` is the better path.

## Conventions

- **JSON descriptors are the source of truth** for exercises, not MIDI files.
- **Engine is pure TS, platform-agnostic.** Audio + pitch adapters are thin wrappers behind interfaces (`AudioPlayer`, `PitchDetector`).
- **Platform resolution** uses Metro's `.web.ts` / `.native.ts` extension lookup. `index.ts` is the web/default entry; `index.native.ts` overrides on iOS/Android.
- **Cents** = 1/100 of a semitone (so 100¢ = 1 piano key). The postprocessor returns `PitchSample` with two fields that must be combined: `midi` (rounded to nearest semitone) + `cents` (signed within-semitone deviation, −50..+50). Deviation from a target MIDI is therefore `(sample.midi - targetMidi) * 100 + (sample.cents ?? 0)`. **Forgetting the `+ sample.cents` term is a silent bug** — pass-detection still works on the right semitone, but every recorded cents value collapses to 0, threshold differences (Strict vs Ballpark) become indistinguishable, and the user can never see how flat or sharp they were within the semitone. Search the codebase for any `(sample.midi - …) * 100` pattern that's missing the cents term.
- **Accuracy %** = fraction of clarity-passed frames within ±50¢ of the target during the inner 60% of the note's duration (skipping attack/release transients).
- **Scoring is post-pattern, not real-time.** During a key, samples accumulate in a buffer; on key end, `alignAndScore()` finds N stable pitch segments, matches them to the expected pattern via DP alignment, and produces N `NoteScore`s. This eliminates per-exercise latency calibration sensitivity — the Tracker has no latency-offset knob; the calibration UI was removed during the pattern-alignment refactor.
- **Tone.Transport** is used for scheduling on web so events can be cancelled by ID — never schedule directly via `triggerAttackRelease(time)` if cancellation matters.
- **Native pitch-shift cap is ±6 semitones** from the nearest sample (`player.native.ts`). Beyond that, decoded `AudioBuffer` artifacts become audible — add a sample, don't widen the cap.
- **Sub-agent worktrees can't see untracked files.** If you spawn agents in `.claude/worktrees/agent-*` and the main repo has substantial uncommitted code, the worktree branches will be empty parallel-universe scaffolds. Either commit first, or run agents directly in the main working tree (only safe when their file scopes don't overlap).
- **DI seam, not `moduleNameMapper`.** Audio + pitch factories are swappable per-test via the registry exports (`__setAudioPlayerFactory`, `__setPitchDetectorFactory`). Use this in component tests (`installFakeAudio()` / `installFakePitch()` in `test/setup-component.ts`); reset auto-runs in `afterEach`. Don't add `jest.mock("@/lib/audio")` to test files — it's no longer needed and conflicts with the registry.
- **Pitch fixtures live in `test/fixtures/pitchSamples.ts`.** When writing a scoring/tracker/diagnose test, prefer `inTune(targets)` / `flat(targets, 50)` / `octaveOff(targets, idx)` / `falseStart(targets)` over hand-rolled `PitchSample[]`. They produce realistic shape (50 fps, clarity 0.92, monotonic timestamps) that matches what a real detector emits.
- **One short comment max** per non-obvious decision. No multi-line docstrings. No "added for X" / "used by Y" notes.

## Active design questions / next features

**Standard-mode sync investigation resolved architecturally.** After E/F/G/H + POC 1 + sing-along calibration + octave-snap, the system was *still* sensitive to per-exercise tempo and global timing offset. The real fix landed as a model change: pattern-alignment scoring (`lib/scoring/align.ts`) — buffer all samples per key, segment, DP-match to expected pattern, score each segment against its musical-context target. The latency-offset slider, the Calibrate button, and `Tracker.setLatencyOffsetMs` were all removed in the post-refactor cleanup; pattern-alignment makes them moot.

See ROADMAP.md §Sprint-Ready Slices for other live candidates:

- Slice A — iOS dev build + on-device validation (M1 finisher; bundles the known native clock-mismatch fix in `detector.native.ts:82`)
- ~~Slice B — Coaching deep-link from session history~~ — shipped as part of regimen-sprint slice 5
- Slice C — Coaching polish (tuning meter + auto-advance) (M2)
- Slice D — Four more PRD exercises + direction-reversal toggle (M4)
- **Coaching-orphan cleanup** (deferred from regimen sprint slice 1) — when a Standard parent is discarded, child coaching sessions tied to it via `parentSessionId` get orphaned in storage. Add a prune pass or change child-session save behavior to mirror the parent's opt-in.

Longer-term: vowel-shape diagram, voice-led chord inversions, siren/glissando exercise type, mic audio recording for true "your version" playback.
